Bootstrap: docker
From: ubuntu:16.04

%post
apt-get update && \
   apt-get install -y r-base openjdk-8-jre wget bwa=0.7.12-5

version=1.8.1

cd /opt && \
   wget -N https://github.com/PapenfussLab/gridss/releases/download/v${version}/gridss-${version}-gridss-jar-with-dependencies.jar \
   && mv gridss-${version}-gridss-jar-with-dependencies.jar gridss-${version}.jar

